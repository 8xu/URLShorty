
<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<meta http-equiv="X-UA-Compatible" content="ie=edge" />
		<link
			rel="stylesheet"
			href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css"
		/>
		<title>URL Shorty</title>
	</head>
	<body>
		<footer class="position-relative d-flex justify-content-center">
			<p class="text-md-center">This project is open source and available <a href="https://github.com/8xu/URLShorty" target="_blank" class="text-danger font-italic">here</a></p>
		</footer>
		<div class="container">
			<div id="header" class="text-center">
				<h1>URLShorty</h1>
				<p>Ever wanted shorter URLs? We got you! 🤛🏽</p>	
			</div>

			<form action="/shorten" method="POST" class="my-4 form-inline">
				<label for="url" class="sr-only">URL</label>
				<input
					required
					placeholder="URL"
					type="url"
					name="url"
					id="url"
					class="form-control col mr-2"
				/>
				<button 
					class="btn btn-success" type="submit">shorten it!</button>
			</form>
			<p class="text-center">Total shortened: <%= total %></p>
			<table class="table-hover table table-striped table-responsive">
				<thead>
					<tr>
						<th scope="col">Full URL</th>
						<th scope="col">Short URL</th>
						<th scope="col">Clicks</th>
					</tr>
				</thead>
				<tbody>
					<% shortUrls.forEach(shortUrl => { %>
					<tr>
						<td><a href="<%= shortUrl.originalUrl %>" target="_blank"><%= shortUrl.originalUrl %></a></td>
						<td><a href="<%= shortUrl.shortUrl %>" target="_blank"><%= shortUrl.shortUrl %></a></td>
						<td><%= shortUrl.clicks %></td>
					</tr>
					<% }) %>
				</tbody>
			</table>

		</div>
	</body>
</html>